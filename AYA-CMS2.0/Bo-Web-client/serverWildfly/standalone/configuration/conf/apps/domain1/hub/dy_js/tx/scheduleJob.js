class scheduleJob extends Application.getCoreTx() {
    runTest = () => {
        //hàm đọc và thông báo người dùng khi lượng data không lấy hết về từ client
        console.log("Có chạy=>",this.input);
		return true;
    }
    runScheduleFromFO = () => {
        //hàm đọc và thông báo người dùng khi lượng data không lấy hết về từ client
        //console.log("Có chạy runScheduleFromFO=>",this.input,this.context);
    }
}
scheduleJob;